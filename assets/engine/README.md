# Bundled engine slot

Place a UCI engine binary named `stockfish` in this directory to ship VChess with
a default engine. When present, it is used automatically (see engine resolution
order in the main README).

The binary itself is not tracked by git.
